{
  "name": "svenska-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run dictionary:build && tsc -b && vite build",
    "dev": "npm run dictionary:build && vite",
    "dictionary:build": "tsx scripts/build-dictionary.ts",
    "test:browser": "node scripts/run-browser-tests.mjs",
    "test:browser:release": "playwright test",
    "release:check": "tsx scripts/validate-dictionary-release.ts",
    "test": "vitest run",
    "test:vitest:browser": "vitest run --config vitest.browser.config.ts",
    "test:watch": "vitest",
    "typecheck": "npm run dictionary:build && tsc -b --pretty false"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "fast-xml-parser": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitest/browser-playwright": "^5.0.1",
    "tsx": "latest",
    "typescript": "latest",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "latest"
  }
}
